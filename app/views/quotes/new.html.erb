<h1>Add a quote</h1>

<%= form_for(@quote) do |f| %>
    <%= f.text_field(:body, {placeholder: "Enter quote"}) %>
    <%= f.text_field(:author, {placeholder: "Author"}) %>
    <%= f.submit %>
<% end %>
